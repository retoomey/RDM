The storage folder will contain implementations of the
disk storage layer.

pq --legacy ldm style xdr stored circular queue

We could experiment with other storage systems,
or you could if you have cool ideas.

